diff --git a/public/sites/all/modules/contrib/multistep/multistep.module b/public/sites/all/modules/contrib/multistep/multistep.module
index d31b0aa..a4eee5e 100644
--- a/public/sites/all/modules/contrib/multistep/multistep.module
+++ b/public/sites/all/modules/contrib/multistep/multistep.module
@@ -174,6 +174,19 @@ function multistep_block_view($type = '') {
     }
     $nid = 0;
   }
+  elseif ($page_callback == 'page_manager_node_add') {
+    if ($router_item['page_arguments'][0] != $type) {
+      return;
+    }
+    $nid = 0;
+  }
+  elseif ($page_callback == 'page_manager_node_edit') {
+    $node = menu_get_object();
+    if ($node->type != $type) {
+      return;
+    }
+    $nid = $node->nid;
+  }
   else {
     return;
   }
